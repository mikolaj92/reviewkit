# ReviewKit 0.23.2

`DocxComment` exposes the balloon range in the host paragraph:
`start_offset` / `end_offset`. Both are set when `anchor_text` occurs
exactly once; otherwise they stay `None` (no guess on a duplicate span).
