# ReviewKit 0.23.3

Reviewed DOCX comments hang on the resolved action span
(`start_offset` / `end_offset`), not a first-match quote. `read_comments`
reads that range from Word `commentRange` markers, so a repeated sentence
still has a unique place.
